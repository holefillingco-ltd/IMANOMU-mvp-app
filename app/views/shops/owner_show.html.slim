= javascript_include_tag "owner_show"

h1 = @shop.name
- imanomu_count = 0
- @shop.tables.each do |table|
  - imanomu_count += table.imanomus.count

h2 送られたIMANOMU
h2 = imanomu_count

== render 'shop' + @shop.id.to_s

